# Eval Fixtures

These projects are intentionally tiny. Some fixture inputs include junk-like paths such as `.codex-*`, `.temp/`, and `__MACOSX/` so the verified rewrite eval can prove those files are excluded from generated rewrite packages.
